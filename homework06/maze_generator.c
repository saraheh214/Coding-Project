#include "maze_generator.h"

void generate_maze( maze* the_maze, const char* key_string ){

    // Delete these 2 lines of code (include so initial compilation works only)
    (void)the_maze;
    (void)key_string;

    // Your work goes here

}

void generate_cell(maze_cell* curr_cell, const char* key_string, int* curr_id, int* char_loc){

    // Delete these 4 lines of code (include so initial compilation works only)
    (void)curr_cell;
    (void)key_string;
    (void)curr_id;
    (void)char_loc;

    // Your work goes here

}

void print_maze( maze_cell* curr_cell ){

    // Delete this line of code (include so initial compilation works only)
    (void)curr_cell;

    // Your work goes here

}

void delete_maze(maze_cell* curr_cell){

    // Delete this line of code (include so initial compilation works only)
    (void)curr_cell;

    // Your work goes here

}
