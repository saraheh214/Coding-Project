#include <stdio.h>

int main( void ){

	/* Create Variable */
	unsigned int needed_wins;

	/* While Loop Statement */
	for(needed_wins =0; needed_wins <= 11; ++needed_wins ){

		fprint(stdout, "ND must win %u wins to make playoff", 11-needed_wins)

	}
}
